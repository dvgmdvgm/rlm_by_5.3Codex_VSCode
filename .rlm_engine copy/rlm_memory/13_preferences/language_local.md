# 🌍 Language Setting (Local Override)

This file overrides `language.md` for LOCAL use only.
It is excluded from git (.gitignore) so it never reaches GitHub.

```
LANGUAGE=ru
```

> This file takes priority over `language.md`.
> If this file exists, AI uses this language.
> If not, AI falls back to `language.md`.
